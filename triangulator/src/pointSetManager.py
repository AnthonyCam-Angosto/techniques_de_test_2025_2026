import subprocess


def appel(id):
    """
    Params:
        id(str): identifiant d'un PointSet

    Returns:
        code_erreur/pointSet(int/String): de base retourne un string mais sinon retourne un chiffre correspondant au type d'erreur
    1= BD indisponible, 2= id non trouver
    """
    pass